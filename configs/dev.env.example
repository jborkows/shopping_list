ADDR=:8080
BASE_URL=http://localhost:8080
DB_DSN=file:data/shopping.db?cache=shared&mode=rwc&_pragma=foreign_keys(1)

# OIDC (Tailscale tsidp)
OIDC_ISSUER=
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_REDIRECT_URL=http://localhost:8080/oauth2/callback

# Dev-only: skip OIDC
AUTH_DISABLED=1

# Admin access: if your OIDC token includes claim `admin=true`, that grants admin automatically.
# Optional fallback: comma-separated emails allowed to access /admin/* endpoints (ignored when AUTH_DISABLED=1)
ADMIN_EMAILS=

HTMX_SRC=https://unpkg.com/htmx.org@1.9.12
